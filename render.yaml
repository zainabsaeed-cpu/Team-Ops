services:
  - type: web
    name: teamops-backend
    env: node
    plan: free
    buildCommand: cd teamops-backend && npm install
    startCommand: cd teamops-backend && npm start
    autoDeploy: true
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        sync: false

  - type: web
    name: teamops-frontend
    runtime: static
    plan: free
    buildCommand: cd teamops-frontend && npm install && npm run build
    staticPublishPath: teamops-frontend/dist
    autoDeploy: true
    envVars:
      - key: VITE_API_URL
        sync: false
      - key: VITE_SOCKET_URL
        sync: false
